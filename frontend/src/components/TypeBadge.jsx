export default function TypeBadge({ type }) {
  return (
    <span className="text-[11px] font-bold px-2.5 py-0.5 rounded-sm bg-blue-50 text-blue-700 border border-blue-200 tracking-wide">
      {type}
    </span>
  );
}
